# Pins the test-suite's API URL so `npm test` stays deterministic regardless
# of a developer's local .env (which sets VITE_API_URL=/api for the Vite dev
# proxy). Tests assert against this literal value.
VITE_API_URL=http://127.0.0.1:8000
